import {
    Button,
    Select,
    TextField,
    Card
} from '@material-ui/core';

const materialComponents = {
    Button,
    Select,
    TextField,
    Card
};

export default materialComponents;